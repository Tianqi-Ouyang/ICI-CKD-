# Data files (contain PHI — never commit)
*.xlsx
*.xls
*.csv
*.rds

# Rendered outputs
*.html
*_files/
*_cache/

# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# Quarto
/.quarto/
_freeze/
# _site/ is NOT ignored when publishing via manual push; keep it out of main branch
# if using `quarto publish gh-pages`, which handles the branch split automatically

# macOS
.DS_Store

# rsconnect deployment records
rsconnect/

**/*.quarto_ipynb
